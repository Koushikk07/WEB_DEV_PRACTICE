Creating the Model

chat with have:  (_id,from,to,message,created_at);

chat -- update --delete  {RESTFUL API}

we can create schemas in seprate folder(files)  for large scale

const mongoose = require("mongoose");

const chatSchema = new mongoose.Schema({
  from: {
    type: String,
  },
  to: {
    type: String,
  },
  msg: {
    type: String,
    maxLength: 50,
  },
  created_at: {
    type: Date,
    required: true,
  },
});

const Chat = mongoose.model("Chat", chatSchema);
module.exports = Chat;


Initialize Database

init.js

chatS

Get  /chats  // show all chats


new and create route

GET /chats/new   --> form render (message/chat)   call by button

POST /chats  -- DB insert chat


Edit & update route

GET /chats/:id/Edit

PUT  /chats/:id

destroy

delete /chats/:id